export interface Position {
  /**
   * Relative X position of image center in canvas.
   * 0.5 means center imager in center of canvas.
   */
  x: number;
  /**
   * Relative Y position of image center in canvas.
   * 0.5 means center imager in center of canvas.
   */
  y: number;
}
